# Public client settings ONLY, never a service_role / secret key.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
AUTH_DOMAIN=users.neo-fit.invalid
# Local preview only. For production set to false.
ALLOW_LOCAL=true
